# Operator Controls

Important controls: fallback to control, kill model, backpressure source, invalidate candidate/cache, rerun freshness, inspect serving trace, pause experiment.

## Recovery mindset

Prefer bounded recovery over broad resets. Preserve evidence before cleanup.
