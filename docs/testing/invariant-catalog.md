# Invariant Catalog

Blocked candidates must not appear. Killed models must not serve silently. Stale features must be visible. Experiments must be able to fallback. Recovery must leave evidence.

## Why it matters

Tests are not only correctness checks here. They are proof that the architecture story is real.
