# API Tour

The API should be read as product workflows, not database operations: create profiles, add graph actions, record interactions, retrieve candidates, refresh feeds, run ranking, assign experiments, train/register models, serve recommendations, ingest realtime feedback, and apply guardrails.

## Useful principle

A good API gives operators and engineers enough evidence to understand what changed and why.
